name: tund_gui
description: Minimal Windows GUI launcher for Tund.
publish_to: "none"
version: 1.8.0+8

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
