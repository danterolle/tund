name: Build & Release

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

jobs:
  linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.6'
          channel: stable
          cache: true
      - run: sudo apt-get update && sudo apt-get install -y clang cmake ninja-build pkg-config libgtk-3-dev liblzma-dev
      - run: make
      - run: make test
      - run: make tools
      - run: make sanitize
      - name: Build GUI
        run: |
          cd gui
          flutter config --enable-linux-desktop
          flutter pub get
          flutter analyze
          flutter test
          flutter build linux --release
          cp ../tund-cli build/linux/x64/release/bundle/tund-cli
          tar -C build/linux/x64/release/bundle -czf ../tund-gui-linux-x86_64.tar.gz .
      - uses: actions/upload-artifact@v4
        with:
          name: tund-cli-linux-x86_64
          path: tund-cli
      - uses: actions/upload-artifact@v4
        with:
          name: tund-gui-linux-x86_64
          path: tund-gui-linux-x86_64.tar.gz

  macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.6'
          channel: stable
          cache: true
      - run: make
      - run: make test
      - run: make tools
      - run: make sanitize
      - name: Build GUI
        run: |
          cd gui
          flutter config --enable-macos-desktop
          flutter pub get
          flutter analyze
          flutter test
          flutter build macos --release
          cp ../tund-cli build/macos/Build/Products/Release/tund-gui.app/Contents/MacOS/tund-cli
          ditto -c -k --sequesterRsrc --keepParent build/macos/Build/Products/Release/tund-gui.app ../tund-gui-darwin-arm64.zip
      - uses: actions/upload-artifact@v4
        with:
          name: tund-cli-darwin-arm64
          path: tund-cli
      - uses: actions/upload-artifact@v4
        with:
          name: tund-gui-darwin-arm64
          path: tund-gui-darwin-arm64.zip

  windows:
    runs-on: windows-latest
    defaults:
      run:
        shell: msys2 {0}
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.6'
          channel: stable
          cache: true
      - uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            mingw-w64-x86_64-gcc
            make
            unzip
            zip
            curl
      - run: make
      - run: ./tund-cli.exe --help
      - run: make tools
      - name: Build GUI
        shell: pwsh
        run: |
          cd gui
          flutter config --enable-windows-desktop
          flutter pub get
          flutter analyze
          flutter test
          flutter build windows --release
      - name: Download wintun.dll
        shell: msys2 {0}
        env:
          WINTUN_URL: https://www.wintun.net/builds/wintun-0.14.1.zip
          WINTUN_SHA256: 07c256185d6ee3652e09fa55c0b673e2624b565e02c4b9091c79ca7d2f24ef51
        run: |
          curl -fsSL "$WINTUN_URL" -o /tmp/wintun.zip
          echo "$WINTUN_SHA256  /tmp/wintun.zip" | sha256sum -c -
          unzip -o /tmp/wintun.zip wintun/bin/amd64/wintun.dll -d /tmp >/dev/null
          cp /tmp/wintun/bin/amd64/wintun.dll .
      - name: Package Windows release
        run: |
          cp gui/build/windows/x64/runner/Release/tund-gui.exe .
          cp gui/build/windows/x64/runner/Release/flutter_windows.dll .
          cp -R gui/build/windows/x64/runner/Release/data .
          zip -r tund-windows-x86_64.zip tund-cli.exe tund-gui.exe flutter_windows.dll data wintun.dll
      - uses: actions/upload-artifact@v4
        with:
          name: tund-windows-x86_64
          path: tund-windows-x86_64.zip

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [linux, macos, windows]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
      - run: |
          mkdir release
          cp tund-cli-linux-x86_64/tund-cli            release/tund-cli-linux-x86_64
          cp tund-cli-darwin-arm64/tund-cli            release/tund-cli-darwin-arm64
          cp tund-gui-linux-x86_64/tund-gui-linux-x86_64.tar.gz release/
          cp tund-gui-darwin-arm64/tund-gui-darwin-arm64.zip release/
          cp tund-windows-x86_64/tund-windows-x86_64.zip release/
      - uses: softprops/action-gh-release@v2
        with:
          files: release/*
